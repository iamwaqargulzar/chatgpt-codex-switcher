{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "cf-gate",
  "description": "Allows the hidden chatgpt.com gate page to report fetch results back (emit events only)",
  "windows": ["cf-gate"],
  "remote": {
    "urls": ["https://chatgpt.com"]
  },
  "permissions": ["core:event:allow-emit"]
}
